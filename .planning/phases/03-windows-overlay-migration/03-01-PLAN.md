---
phase: 03-windows-overlay-migration
plan: 01
type: execute
---

<objective>
Port macOS overlay rendering system to Windows, ensuring shared React UI works with platform-specific CEF layer.

Purpose: Replace Windows' old overlay system with the new unified system from macOS, achieving cross-platform consistency with shared frontend code.
Output: Windows overlay rendering system updated to match macOS patterns, ready for build and testing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-windows-overlay-migration/03-CONTEXT.md

# Prior phase summaries (dependency graph)
@.planning/phases/1-complete-macos-wallet-ui/01-02-SUMMARY.md
@.planning/phases/02-devtools-integration/02-01-SUMMARY.md

# Key files from prior phases
@cef-native/src/handlers/my_overlay_render_handler.cpp
@cef-native/src/handlers/my_overlay_render_handler.mm
@cef-native/include/handlers/my_overlay_render_handler.h
@cef-native/cef_browser_shell.cpp
@frontend/src/pages/WalletOverlayRoot.tsx

# Codebase context
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

**Tech stack available:**
- CEF 136 with windowless rendering
- Windows: UpdateLayeredWindow, DWM composition
- macOS: NSView with CALayer
- React 19 + MUI 7 (shared UI)

**Established patterns:**
- Platform-specific #ifdef blocks for Windows/macOS
- Shared React components across platforms
- my_overlay_render_handler with .cpp (Windows) and .mm (macOS)
- Overlay windows use CEF windowless mode with custom rendering

**Constraining decisions:**
- Phase 01-02: Tabbed interface for advanced features (must work on Windows)
- Phase 02-01: DevTools available on all windows (must work on ported overlays)
- Context: Shared React UI, platform-specific CEF layer
- Context: Use Windows conventions where appropriate (window chrome, shadows)

**Issues being addressed:** None specific, but ensuring Windows overlay parity with Mac
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Windows overlay render handler</name>
  <files>cef-native/src/handlers/my_overlay_render_handler.cpp</files>
  <action>
Examine the macOS implementation in my_overlay_render_handler.mm and ensure the Windows implementation in my_overlay_render_handler.cpp follows the same logical flow:

1. **Constructor**: Verify Windows uses UpdateLayeredWindow with DWM composition (already present), matching macOS's CALayer approach conceptually
2. **GetViewRect**: Ensure returns correct dimensions for windowless rendering
3. **OnPaint**:
   - Verify buffer copy to DIB section is correct
   - Ensure UpdateLayeredWindow call properly updates the window with transparency
   - Check that alpha channel handling matches CEF's BGRA format expectations
4. **Destructor**: Verify proper cleanup of HDC, HBITMAP resources

DO NOT change the fundamental Windows API usage (UpdateLayeredWindow, CreateDIBSection) - these are correct. Focus on ensuring the logic flow matches the working macOS version.

AVOID: Changing CEF lifecycle methods without understanding render process threading. The current OnPaint signature and CEF callback pattern must remain unchanged.

WHY: The macOS version works correctly with the new React overlay UI. Windows must have equivalent behavior with platform-specific rendering APIs.
  </action>
  <verify>
1. Read my_overlay_render_handler.cpp and my_overlay_render_handler.mm side by side
2. Confirm Windows #ifdef blocks have equivalent logic to macOS #elif blocks
3. Check that constructor, GetViewRect, OnPaint, destructor are structurally parallel
4. No compilation errors when reading through C++ logic
  </verify>
  <done>
- Windows overlay render handler code reviewed and updated if needed
- Logic flow matches macOS implementation conceptually
- Platform-specific APIs (UpdateLayeredWindow vs CALayer) are correctly used
- No obvious missing steps compared to macOS version
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify overlay window integration in main shell</name>
  <files>cef-native/cef_browser_shell.cpp</files>
  <action>
Verify that the Windows main shell correctly integrates with the overlay render handler:

1. **Global HWNDs**: Confirm overlay window globals are declared (g_settings_overlay_hwnd, g_wallet_overlay_hwnd, g_backup_overlay_hwnd, g_brc100_auth_overlay_hwnd)
2. **Window Creation**: Check WndProc or window creation code uses WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_TOPMOST for overlay windows
3. **Render Handler Assignment**: Verify CefBrowserHost::CreateBrowser uses MyOverlayRenderHandler for overlay browser instances
4. **Window Lifecycle**: Confirm proper cleanup in shutdown sequence (DestroyWindow calls)

Check that the pattern matches how macOS creates overlay NSWindows (conceptually - the API calls will differ).

AVOID: Changing the main browser window creation - only overlay windows are in scope. Do NOT modify CEF message loop or threading initialization.

WHY: Overlay windows must be created as layered windows with the custom render handler to support transparency and proper z-ordering.
  </action>
  <verify>
1. Read cef_browser_shell.cpp focusing on overlay-related code
2. Grep for "g_.*_overlay_hwnd" to find all overlay window references
3. Check that overlay windows use WS_EX_LAYERED style
4. Verify MyOverlayRenderHandler is instantiated for overlay browsers
  </verify>
  <done>
- Global overlay HWNDs are properly declared and initialized
- Overlay windows created with correct window styles (WS_EX_LAYERED)
- MyOverlayRenderHandler correctly assigned to overlay browser instances
- Cleanup code properly destroys overlay windows on shutdown
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] my_overlay_render_handler.cpp Windows code reviewed and matches macOS logic flow
- [ ] cef_browser_shell.cpp overlay integration verified
- [ ] No breaking changes to CEF lifecycle or threading
- [ ] Code is ready for Windows build (no obvious compile errors)
</verification>

<success_criteria>

- All tasks completed
- Windows overlay rendering code updated to match macOS patterns
- Overlay window integration in main shell verified
- No changes to CEF lifecycle/threading (fragile areas)
- Ready for Windows build in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-windows-overlay-migration/03-01-SUMMARY.md`:

# Phase 3 Plan 1: Port Overlay Rendering System Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `cef-native/src/handlers/my_overlay_render_handler.cpp` - Description of changes
- `cef-native/cef_browser_shell.cpp` - Description of changes

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 03-02-PLAN.md (Build and Functional Verification)
</output>
