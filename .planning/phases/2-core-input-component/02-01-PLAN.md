---
phase: 02-core-input-component
plan: 01
type: execute
---

<objective>
Build foundational omnibox React component with instant dropdown UI and Material-UI styling.

Purpose: Replace simple InputBase with Chrome-like autocomplete component that provides smooth, instant feedback and clean architecture for future phases (history autocomplete, Google search integration).
Output: Working Omnibox component integrated into MainBrowserView with mock suggestions, dropdown animation, and polished Material-UI design.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1-foundation-investigation/INVESTIGATION.md
@.planning/phases/2-core-input-component/2-CONTEXT.md
@frontend/src/pages/MainBrowserView.tsx
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

**Tech stack available:**
- React functional components with hooks
- Material-UI (Paper, InputBase, List, ListItem components)
- TypeScript with strict mode

**Established patterns:**
- React components: PascalCase.tsx with default export
- Component props: Destructured with TypeScript interface
- State: useState hooks with camelCase naming
- Event handlers: handleEventName pattern
- Styling: MUI sx prop with inline styles

**Constraining decisions:**
- REPLACE: MainBrowserView InputBase (lines 183-227) - simple text input has no autocomplete dropdown
- KEEP: Paper container pill-shaped styling (borderRadius 20px, hover/focus states)
- KEEP: handleNavigate() navigation flow - same navigate(url) call
- Phase 2 boundaries: Mock data only (no database), no Google API, basic keyboard only (Enter/Escape)

**Context from Phase 1:**
- Existing address bar: InputBase in Paper container (lines 183-227 of MainBrowserView.tsx)
- Paper styling: Pill shape (borderRadius 20), gray background (#f1f3f4), white on hover/focus
- Navigation: handleNavigate() calls navigate(address) from useHodosBrowser hook
- Keyboard: Ctrl+L focuses address bar via addressBarRef
- Tab sync: useEffect (lines 45-53) syncs address from activeTabId when not editing

**Phase 2 vision (from 2-CONTEXT.md):**
- Chrome-style instant dropdown - appears immediately when typing
- Smooth, polished feel with zero lag
- Material-UI native aesthetic (not pixel-perfect Chrome clone)
- Clean component architecture for easy extension in Phases 3-5
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Omnibox component with dropdown UI</name>
  <files>frontend/src/components/Omnibox.tsx</files>
  <action>
Create new React component at frontend/src/components/Omnibox.tsx using Material-UI:

**Component structure:**
- Default export: function Omnibox(props: OmniboxProps)
- Props interface: { onNavigate: (url: string) => void, initialValue?: string }

**State management:**
- inputValue: string (text input value)
- showDropdown: boolean (dropdown visibility)
- suggestions: string[] (mock data array)
- selectedIndex: number (for future arrow key navigation, initialize to -1)

**Mock suggestions array:**
['https://google.com', 'https://github.com', 'https://wikipedia.org', 'https://stackoverflow.com', 'https://reddit.com']

**UI structure:**
- Root Box container (relative positioning for dropdown)
- Paper container with pill styling (copy from MainBrowserView lines 184-204):
  - borderRadius: 20
  - bgcolor: '#f1f3f4'
  - hover: white background, subtle border
  - focus-within: white background, blue border (#1a73e8), blue shadow
- InputBase inside Paper:
  - value={inputValue}
  - onChange: update inputValue, show dropdown if value.length > 0
  - onFocus: select all text
  - onBlur: hide dropdown after 200ms delay (allow clicking suggestions)
  - onKeyDown: Enter → call onNavigate(inputValue), Escape → clear input and hide dropdown
  - placeholder: "Search or enter address"
  - fullWidth, fontSize 14
- Dropdown Paper (absolute position below input):
  - Show when showDropdown && suggestions.length > 0
  - Position: absolute, top 100% + 8px, left 0, right 0, zIndex 1000
  - borderRadius: 8px
  - boxShadow: Material-UI elevation 8
  - List with suggestions.map() → ListItem
  - ListItem hover: light gray background (#f5f5f5)
  - ListItem onClick: call onNavigate(suggestion), hide dropdown
  - Use Fade or Collapse for smooth dropdown animation

**Behavior:**
- Initialize inputValue from initialValue prop if provided
- Show dropdown immediately when typing (inputValue.length > 0)
- Hide dropdown when Escape pressed or input blurred
- Filter suggestions: suggestions.filter(s => s.includes(inputValue.toLowerCase()))
- If no matches, don't show dropdown

**Styling approach:**
- Use Material-UI sx prop for all styling
- Follow MUI design language (don't try to pixel-match Chrome)
- Ensure visual consistency with existing browser UI
- Smooth transitions for dropdown show/hide

**What to avoid and WHY:**
- DON'T add arrow key navigation (ArrowUp/Down) - deferred to Phase 5 for focused scope
- DON'T query HistoryManager or database - Phase 3 feature, keep mock data only
- DON'T implement URL vs search detection - Phase 4 feature
- DON'T use custom CSS classes or styled-components - use MUI sx prop for consistency
- DON'T try to match Chrome's exact visual design - embrace MUI aesthetic
- DON'T add Tab autocomplete - Phase 5 feature

**TypeScript:**
- Use strict typing for props interface
- Type state variables explicitly
- Follow conventions: 2-space indent, single quotes, camelCase variables
  </action>
  <verify>
- File exists: frontend/src/components/Omnibox.tsx
- npx tsc compiles without errors
- Component exports: export default function Omnibox
- Has OmniboxProps interface with onNavigate: (url: string) => void
- Imports Material-UI: Box, Paper, InputBase, List, ListItem, Fade (or Collapse)
  </verify>
  <done>
- Omnibox component created with InputBase + dropdown Paper
- Mock suggestions array defined
- Dropdown shows/hides based on input value
- Enter key triggers onNavigate callback
- Escape clears input and hides dropdown
- Material-UI styling applied (pill shape, hover states, shadows)
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Omnibox into MainBrowserView</name>
  <files>frontend/src/pages/MainBrowserView.tsx</files>
  <action>
Replace existing InputBase with new Omnibox component in MainBrowserView:

**Import statement:**
Add at top: import Omnibox from '../components/Omnibox';

**Remove old address bar (lines 183-227):**
- Delete entire Paper + InputBase block (lines 183-227)
- Keep surrounding IconButtons (Back/Forward/Refresh and Wallet/History/Settings)

**Add Omnibox component:**
Replace deleted section with:
```tsx
<Omnibox
  onNavigate={handleNavigate}
  initialValue={address}
/>
```

**State cleanup:**
- KEEP: const [address, setAddress] = useState('https://metanetapps.com');
  Reason: Tab sync useEffect (lines 45-53) still needs this to sync from active tab
- KEEP: const [isEditingAddress, setIsEditingAddress] = useState(false);
  Reason: May be needed for tab sync logic
- REMOVE: const addressBarRef = useRef<HTMLInputElement>(null);
  Reason: Omnibox manages its own ref internally
- UPDATE: useKeyboardShortcuts - remove or update onFocusAddressBar (Ctrl+L won't work until Omnibox exposes focus method)

**Event handler cleanup:**
- REMOVE: handleAddressFocus (lines 84-88) - Omnibox handles internally
- REMOVE: handleAddressBlur (lines 90-97) - Omnibox handles internally
- UPDATE: handleKeyDown (lines 71-82) - remove entire function, Omnibox handles keyboard
- KEEP: handleNavigate (lines 66-69) - pass to Omnibox as onNavigate prop

**Tab sync adjustment (lines 45-53):**
Current code syncs address from activeTab.url when not editing. Options:
1. Keep as-is (address state updates but Omnibox ignores it initially)
2. Update Omnibox to accept controlled value prop (more complex)
3. Disable for now and address in Phase 3 (when real data integration happens)

Choose option 3: Comment out the useEffect for now with TODO comment explaining it will be re-enabled in Phase 3 when Omnibox integrates with tab state.

**Testing checklist:**
After changes, verify:
- npm run build succeeds
- npx tsc passes without errors
- Omnibox renders in toolbar
- Typing shows dropdown with mock suggestions
- Enter key triggers navigation
- Escape clears input
- Navigation flow works (handleNavigate is called correctly)

**What to avoid and WHY:**
- DON'T try to make Omnibox a controlled component yet - let it manage internal state independently for this phase (Phase 3 will add proper state synchronization)
- DON'T add new IPC messages - Phase 3 feature for database queries
- DON'T modify useHodosBrowser hook - navigation API is correct as-is
- DON'T spend time on Ctrl+L focus - acceptable to break temporarily, will be fixed in Phase 5 when keyboard navigation is implemented properly
  </action>
  <verify>
- npm run build succeeds without errors
- npx tsc compiles without errors
- MainBrowserView imports Omnibox component
- Omnibox replaces old Paper + InputBase (lines 183-227 removed)
- Omnibox receives onNavigate={handleNavigate} prop
- handleAddressFocus, handleAddressBlur, handleKeyDown removed
- addressBarRef removed or commented out
- Tab sync useEffect commented out with TODO for Phase 3
  </verify>
  <done>
- Omnibox integrated into MainBrowserView
- Old InputBase removed
- Event handlers cleaned up
- TypeScript compiles successfully
- Navigation works (Enter → handleNavigate → navigate)
- Dropdown shows mock suggestions when typing
- Component is functional and ready for manual testing
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds without errors
- [ ] npx tsc passes with no TypeScript errors
- [ ] File exists: frontend/src/components/Omnibox.tsx
- [ ] MainBrowserView imports and uses Omnibox component
- [ ] Typing in omnibox shows dropdown with filtered mock suggestions
- [ ] Enter key triggers navigation (handleNavigate called)
- [ ] Escape key clears input and hides dropdown
- [ ] Dropdown has smooth animation (Fade or Collapse)
- [ ] Material-UI styling is polished (pill shape, hover states, shadows)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors or build warnings
- Omnibox component has Chrome-like instant dropdown feel
- Material-UI styling is professional and consistent with browser UI
- Component architecture is clean and extensible (ready for Phase 3 database integration)
- Mock suggestions filter based on input and display correctly
- Navigation flow works end-to-end (type → Enter → navigate)
</success_criteria>

<output>
After completion, create `.planning/phases/2-core-input-component/02-01-SUMMARY.md`:

# Phase 2 Plan 1: Core Input Component Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- Created Omnibox React component with Material-UI dropdown and mock suggestions
- Integrated Omnibox into MainBrowserView replacing simple InputBase
- Established clean component architecture for future phases
- Implemented instant dropdown feedback with smooth animations

## Files Created/Modified

- `frontend/src/components/Omnibox.tsx` - New omnibox component with dropdown UI
- `frontend/src/pages/MainBrowserView.tsx` - Integrated Omnibox, removed old InputBase

## Decisions Made

[Document any implementation decisions made during execution, or "None"]

## Issues Encountered

[Document any problems and their resolutions, or "None"]

## Next Phase Readiness

Phase 2 complete. Ready for Phase 3: History Autocomplete.

Omnibox component is structured to easily integrate with HistoryManager database queries - just replace mock suggestions array with real data from IPC calls.
</output>
