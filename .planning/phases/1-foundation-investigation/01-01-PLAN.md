---
phase: 1-foundation-investigation
plan: 01
type: execute
---

<objective>
Map the existing address bar and history database architecture to understand what must be replaced, what can be reused, and how data flows through the three-layer system.

Purpose: Build complete knowledge base of current implementation before designing omnibox replacement. Clear documentation prevents expensive architectural mistakes.
Output: Comprehensive investigation documentation in `.planning/phases/1-foundation-investigation/INVESTIGATION.md` with code inventory, architecture diagrams, database schema, React component hierarchy, and replace-vs-reuse decisions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/1-foundation-investigation/1-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@cef-native/include/core/HistoryManager.h
@cef-native/src/core/HistoryManager.cpp
@frontend/src/pages/MainBrowserView.tsx
@frontend/src/hooks/useHodosBrowser.ts
@frontend/src/bridge/initWindowBridge.ts

**Existing codebase knowledge:**
- Address bar exists in MainBrowserView.tsx (lines 183-227)
- HistoryManager in C++ manages SQLite database
- Three-layer architecture: React → CEF → Rust (wallet only)
- History stored in `%APPDATA%/HodosBrowser/Default/HodosHistory`

**Key constraint from 1-CONTEXT.md:**
Must produce "Clear replace vs keep decisions" - this is the critical outcome that informs all future phases.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document address bar React component structure</name>
  <files>.planning/phases/1-foundation-investigation/INVESTIGATION.md</files>
  <action>
Read MainBrowserView.tsx and all related hooks (useHodosBrowser, useTabManager, useKeyboardShortcuts). Document:
- Component hierarchy for address bar (lines 183-227 in MainBrowserView.tsx)
- State management (address, isEditingAddress)
- Event handlers (handleNavigate, handleKeyDown, handleAddressFocus, handleAddressBlur)
- Material-UI components used (Paper, InputBase)
- Props and data flow
- Tab synchronization logic (lines 45-53)
- Navigation integration with CEF layer

Create section "React Component Architecture" in INVESTIGATION.md with:
1. Component tree diagram (ASCII)
2. State flow diagram
3. Event handler inventory
4. Material-UI component mapping
5. Replace vs Keep analysis for each component
  </action>
  <verify>
INVESTIGATION.md exists with "React Component Architecture" section containing component tree, state flow, handler inventory, MUI mapping, and replace/keep decisions for React layer.
  </verify>
  <done>Documentation shows exactly which React components/hooks are reusable for omnibox vs which need replacement. Developer can understand current frontend architecture from markdown alone.</done>
</task>

<task type="auto">
  <name>Task 2: Document HistoryManager database schema and API</name>
  <files>.planning/phases/1-foundation-investigation/INVESTIGATION.md</files>
  <action>
Read HistoryManager.h and HistoryManager.cpp completely. Document:
- SQLite schema (urls table: id, url, title, visit_count, typed_count, last_visit_time, hidden; visits table: id, url, visit_time, from_visit, transition, segment_id, visit_duration)
- Indexes (idx_urls_url, idx_urls_last_visit_time, idx_visits_url, idx_visits_visit_time)
- C++ API surface (AddVisit, GetHistory, SearchHistory, GetHistoryEntryByUrl, Delete operations)
- Chromium timestamp format (microseconds since 1601, conversion functions)
- Query patterns (JOIN between urls and visits, ORDER BY visit_time DESC)
- Database location (%APPDATA%/HodosBrowser/Default/HodosHistory)
- Initialization pattern (singleton GetInstance)

Create section "History Database Architecture" in INVESTIGATION.md with:
1. Schema diagram (tables, fields, indexes, foreign keys)
2. API method inventory with signatures
3. SQL query examples for autocomplete use case
4. Timestamp format documentation
5. Access pattern for omnibox (how to query for autocomplete suggestions)
6. Replace vs Keep analysis for database layer
  </action>
  <verify>
INVESTIGATION.md contains "History Database Architecture" section with complete schema, API reference, query patterns, and analysis of what's reusable for omnibox autocomplete.
  </verify>
  <done>Documentation shows exact database schema, C++ API methods, and SQL patterns needed for omnibox autocomplete. Developer knows precisely how to query history without reading C++ source.</done>
</task>

<task type="auto">
  <name>Task 3: Map data flow and create replace-vs-reuse decision matrix</name>
  <files>.planning/phases/1-foundation-investigation/INVESTIGATION.md</files>
  <action>
Trace complete data flow from user input to history storage across all three layers. Document:
- User types URL in MainBrowserView.tsx InputBase
- handleNavigate calls navigate() from useHodosBrowser
- navigate() sends IPC message via window.cefMessage.send()
- C++ layer receives message in SimpleHandler::OnProcessMessageReceived
- Browser navigates, triggers page load
- HistoryManager::AddVisit() called on successful load
- SQLite write (INSERT/UPDATE urls, INSERT visits)

Create sections:
1. "Data Flow Diagram" - ASCII diagram showing: React Input → useHodosBrowser → IPC → C++ CEF → Navigation → HistoryManager → SQLite
2. "IPC Protocol" - message names, payloads, handlers
3. "Replace vs Reuse Decision Matrix" - table with Component/Module, Current Purpose, Omnibox Needs, Decision (Replace/Reuse/Extend), Rationale

Decision matrix must cover:
- MainBrowserView address bar component (lines 183-227)
- useHodosBrowser navigation hooks
- HistoryManager C++ class
- SQLite schema
- IPC message protocol
- Tab synchronization logic
  </action>
  <verify>
INVESTIGATION.md contains "Data Flow Diagram", "IPC Protocol", and "Replace vs Reuse Decision Matrix" sections. Matrix has rows for all major components with clear Replace/Reuse/Extend decisions and rationale.
  </verify>
  <done>Documentation includes complete end-to-end data flow and decision matrix that explicitly states what gets replaced (address bar component), what gets reused (HistoryManager, SQLite schema), and what gets extended (IPC protocol for autocomplete). Critical outcome from 1-CONTEXT.md is satisfied.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] INVESTIGATION.md exists in .planning/phases/1-foundation-investigation/
- [ ] Contains "React Component Architecture" section with component tree, state flow, MUI components
- [ ] Contains "History Database Architecture" section with schema, API reference, SQL patterns
- [ ] Contains "Data Flow Diagram" showing full request path
- [ ] Contains "IPC Protocol" documentation
- [ ] Contains "Replace vs Reuse Decision Matrix" with explicit decisions for all major components
- [ ] Decision matrix clearly states what will be replaced vs reused in Phase 2+
- [ ] Documentation is readable markdown with code snippets and ASCII diagrams
</verification>

<success_criteria>

- All tasks completed
- INVESTIGATION.md is comprehensive enough for Phase 2 planning without re-reading source code
- Replace vs reuse decisions are explicit and justified
- No code changes made (pure investigation phase)
- Developer reading INVESTIGATION.md understands: (1) How current address bar works, (2) How HistoryManager database works, (3) What gets replaced for omnibox, (4) What gets reused
  </success_criteria>

<output>
After completion, create `.planning/phases/1-foundation-investigation/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Foundation Investigation Summary

**Mapped existing address bar and history database architecture with clear replace-vs-reuse decisions for omnibox implementation.**

## Accomplishments

- Documented React address bar component structure (MainBrowserView.tsx InputBase, state management, event handlers)
- Documented HistoryManager C++ API and SQLite schema (urls + visits tables with indexes)
- Traced complete data flow from React → IPC → C++ → SQLite
- Created replace-vs-reuse decision matrix for all major components

## Files Created/Modified

- `.planning/phases/1-foundation-investigation/INVESTIGATION.md` - Complete investigation documentation

## Decisions Made

[Document key replace-vs-reuse decisions from matrix, e.g.:
- Replace: MainBrowserView address bar InputBase (lines 183-227) - needs autocomplete dropdown
- Reuse: HistoryManager C++ class and SQLite schema - already provides needed query methods
- Extend: IPC protocol - add new message types for autocomplete suggestions]

## Issues Encountered

[Note any surprises during investigation, or "None"]

## Next Step

Phase complete. Ready to plan Phase 2: Core Input Component.
</output>
