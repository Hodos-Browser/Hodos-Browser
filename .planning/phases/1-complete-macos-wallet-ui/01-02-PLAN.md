---
phase: 1-complete-macos-wallet-ui
plan: 02
type: execute
---

<objective>
Complete Advanced features page and perform comprehensive cross-functional verification of macOS wallet UI.

Purpose: Ensure BRC-100 certificate management is accessible and verify the complete wallet system works end-to-end on macOS.
Output: Functional advanced features page, verified wallet panel operations, and confidence that Phase 1 is complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1-complete-macos-wallet-ui/1-CONTEXT.md
@.planning/phases/1-complete-macos-wallet-ui/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md

**Tech stack available:**
- React 19 with MUI 7 for UI
- Rust Actix-web backend on localhost:3301
- BRC-100 protocol suite (BRC-52 certificates)
- Established overlay navigation pattern

**Established patterns:**
- Advanced button in WalletPanel navigates to /wallet route
- Overlay pages follow *OverlayRoot.tsx naming
- BRC-100 operations through window.hodosBrowser API

**Rust BRC-100 endpoints available:**
- POST /listCertificates - Lists user's identity certificates
- POST /acquireCertificate - Acquires new certificate
- POST /proveCertificate - Proves certificate ownership

**From Plan 01:**
- Balance display working
- Send/Receive operations functional
- useWallet hook established
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Advanced Features page for certificate management</name>
  <files>frontend/src/pages/WalletPanelPage.tsx, frontend/src/hooks/useHodosBrowser.ts</files>
  <action>
    The Advanced button in WalletPanel already navigates to '/wallet' route. Ensure the WalletPanelPage.tsx (or similar advanced features page) exists and displays BRC-100 certificate functionality.

    Implementation:
    1. Locate the advanced features page (likely WalletPanelPage.tsx or WalletOverlayRoot.tsx based on STRUCTURE.md)
    2. Add certificate listing using window.hodosBrowser API or create listCertificates() in useHodosBrowser if missing
    3. Display certificates in simple list (table or MUI List component)
    4. Show: certificate type, verifier, fields, status
    5. Add "Acquire Certificate" button (can be disabled with TODO comment - full acquire flow is complex)
    6. Keep UI minimal for quick depth - focus on displaying existing certificates

    The Rust endpoints exist (/listCertificates, /acquireCertificate), just need frontend wiring.

    Do NOT implement full certificate acquisition flow (complex, involves verifier interaction). Do NOT implement certificate proving UI. Just show what certificates exist and provide placeholder for future "Acquire" functionality.
  </action>
  <verify>
    Advanced features page loads when clicking Advanced button in WalletPanel. Page shows list of certificates (or empty state if none). No TypeScript errors. Console shows successful /listCertificates call.
  </verify>
  <done>
    Advanced features page displays. Certificates listed from Rust backend. UI is simple but functional. Navigation from WalletPanel works. Future certificate acquisition placeholdered.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete macOS wallet UI with core operations and advanced features</what-built>
  <how-to-verify>
    Full end-to-end wallet verification on macOS:

    1. **Setup**: Ensure all three processes running (Rust wallet, frontend dev server, CEF browser)

    2. **Balance Verification**:
       - Open wallet panel
       - Verify balance displays correctly
       - Refresh and verify balance persists

    3. **Receive Verification**:
       - Click Receive button
       - Verify valid BSV address displayed
       - Copy address and verify format (starts with 1 or 3)

    4. **Send Verification** (optional - requires test BSV):
       - Click Send button
       - Enter test recipient address and small amount
       - Verify transaction sends or shows meaningful error
       - Check Rust wallet logs for transaction processing

    5. **Advanced Features Verification**:
       - Click Advanced button
       - Verify navigation to advanced features page
       - Verify certificate list loads (or shows empty state)
       - Check console for successful API calls

    6. **Error Handling**:
       - Try Send with invalid address - verify error shown
       - Try Send with amount > balance - verify error shown
       - Verify no unhandled promise rejections in console

    7. **Cross-Feature Flow**:
       - Navigate: WalletPanel → Advanced → back to browser
       - Verify overlay state management works
       - Check for memory leaks (rapid open/close wallet panel)

    8. **Console Check**:
       - No red errors in browser console
       - Rust wallet logs show successful API calls
       - TypeScript build has no errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues for fixing</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds without errors
- [ ] cargo build --release succeeds (verify no Rust changes broke build)
- [ ] All wallet panel operations functional (balance, send, receive)
- [ ] Advanced features page accessible and functional
- [ ] Certificate listing works (or gracefully shows empty state)
- [ ] No console errors during normal operation
- [ ] Manual verification completed successfully
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Advanced features page wired to Rust certificate endpoints
- Full wallet flow tested end-to-end on macOS
- No TypeScript or Rust compilation errors
- Manual verification approved
- Phase 1 complete: macOS wallet UI fully functional
  </success_criteria>

<output>
After completion, create `.planning/phases/1-complete-macos-wallet-ui/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Advanced Features & Verification Summary

**macOS wallet UI complete and verified - all core operations and certificate management functional**

## Accomplishments

- Advanced features page wired to BRC-100 certificate endpoints
- Certificate listing functional (displays existing certificates)
- Complete end-to-end wallet flow verified on macOS
- All wallet panel operations tested and working
- Phase 1 objectives met: macOS wallet UI at parity with planned functionality

## Files Created/Modified

- `frontend/src/pages/WalletPanelPage.tsx` - Certificate listing and advanced features
- `frontend/src/hooks/useHodosBrowser.ts` - Certificate management methods (if added)

## Decisions Made

[Document any implementation choices made during execution]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

**Blockers for Phase 2**: None

**Concerns**:
- DevTools integration will need keyboard shortcut handling (Phase 2 scope)
- Certificate acquisition flow placeholdered - full implementation deferred

**Ready for Phase 2**: DevTools Integration (keyboard shortcuts and UI access for CEF DevTools on both platforms)

---

## Phase 1 Complete

macOS wallet UI is now functional with:
- Balance display from Rust backend
- Send/Receive operations working
- Advanced features showing BRC-100 certificates
- Error handling in place
- All operations tested end-to-end

Phase 1: Complete macOS Wallet UI ✓
</output>
